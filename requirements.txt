# ============================================================
#  Dependencies สำหรับ GitHub Actions Python runner
#  ใช้เฉพาะไลบรารีที่จำเป็นจริง ๆ เพื่อให้ install เร็ว
# ============================================================

# HTTP client สำหรับดึงข้อมูล OHLCV จาก Bybit และส่ง Telegram/LINE
requests>=2.31.0

# ------------------------------------------------------------
# หมายเหตุ:
#  - numpy/pandas: ไม่จำเป็นเพราะโค้ดใช้ pure Python lists
#    (bar-by-bar loop ตามข้อกำหนดของ Pine Script translation)
#    ถ้าต้องการใช้สำหรับ data analysis ภายหลัง ให้ uncomment:
#
#  numpy>=1.24.0
#  pandas>=2.0.0
#
#  - ccxt: ไม่จำเป็นเพราะเราใช้ Bybit public API ตรง ๆ
#    ถ้าต้องการรองรับหลาย exchange ให้ uncomment:
#
#  ccxt>=4.0.0
# ------------------------------------------------------------
